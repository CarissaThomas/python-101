# Seperate different python environments for different projects
# Ensures that package collisions don't occur across projects

# Creating a virutal environment
# Cd to environment directory or create one
# virtualenv PROJECT_NAME
# source PROJECT_NAME/bin/activate
# which python (shows which environment)
# deactivate (get out of current python environment)
# rm -rm PROJECT_NAME
# virtualenv -p /usr/bin/python2.6 PROJECT_NAME (create environment with certain python version)